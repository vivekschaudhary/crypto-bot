# Project Status

_Last updated: 2026-06-19 (**CB-8 brief PROPOSED** — responsive left-sidebar dashboard nav (replaces the top 3-tab strip; responsive mobile/iPad/laptop + desktop-collapsible). Layout/chrome only; `architecture_required: true` (introduces the project's first CSS layer + @media + viewport meta). Awaiting operator approval → `/create-bet-architecture CB-8`. CB-6 done (8 stories, e2e green, migration 0008 applied to prod + session reset). Operator-side: LIVE_MODE flip; /scan CB-6 Production-Ready; CB-7 (equity/Zerodha); #80. MVP [CB-1..CB-5] + CB-6 complete.)_

## In flight

| Bet | Phase | Owner role | Awaiting | Started | ETA |
|---|---|---|---|---|---|
| [CB-1](bets/CB-1/brief.md) | **SHIPPED 2026-06-05** — all 7 stories (CB-1.1 + CB-1.1.1 + CB-1.2 + CB-1.3 + CB-1.4 + CB-1.5 + CB-1.6) + 1 security follow-up PR. Foundational auth bet complete; downstream bets (CB-2..CB-5) now unblocked. | — | `/measure CB-1` cron picks up + `/create-brief CB-2` promotion is next-on-deck | 2026-05-31 | 2026-06-05 actual (2.3 weeks ahead of plan v5's 06-21) |
| [CB-2](bets/CB-2/brief.md) | **CB-2 SHIPPED 2026-06-08** — All 5 stories via PRs [#26](https://github.com/vivekschaudhary/crypto-bot/pull/26) + [#32](https://github.com/vivekschaudhary/crypto-bot/pull/32) + [#34](https://github.com/vivekschaudhary/crypto-bot/pull/34) + [#39](https://github.com/vivekschaudhary/crypto-bot/pull/39) + [#41](https://github.com/vivekschaudhary/crypto-bot/pull/41). 3 calendar days actual vs 1-week brief estimate. EdDSA brokerage caveat RESOLVED; real-money write path verified; rate-limit headers empirically discovered; all 3 Researcher Open Questions closed. | — | `/measure CB-2` cron picks up; CB-3 unblocked | 2026-06-06 actual | **2026-06-08 actual (5 days ahead of estimate)** |
| [CB-3](bets/CB-3/brief.md) | **CB-3 BET SHIPPED 2026-06-09** — All 4 stories (.0/.1/.2/.3 after CB-3.4 fold) via PRs [#45](https://github.com/vivekschaudhary/crypto-bot/pull/45) + [#47](https://github.com/vivekschaudhary/crypto-bot/pull/47) + [#48](https://github.com/vivekschaudhary/crypto-bot/pull/48) + [#49](https://github.com/vivekschaudhary/crypto-bot/pull/49) + **4 post-merge polish PRs** [#50](https://github.com/vivekschaudhary/crypto-bot/pull/50) (RSC adapter-prop serialization fix) + [#51](https://github.com/vivekschaudhary/crypto-bot/pull/51) (`"use server"` file export purity fix) + [#52](https://github.com/vivekschaudhary/crypto-bot/pull/52) (empty-numeric-input UX `0` trap) + [#53](https://github.com/vivekschaudhary/crypto-bot/pull/53) (labels prop portability for equity-app extraction). **1.5 calendar days actual vs 3 wk medium-confidence brief-approval estimate (~17x faster)**. 554 tests; 9-PR squash-race streak clean. Strategy authoring + persistence + activation surface live in prod at `/dashboard/strategy`. **Researcher Q2 CLOSED** at story drafting (RSI period not exposed in form; CB-4 hardcodes RSI(14)). **Researcher Q1 SEEDED, NOT YET CLOSED** — first empirical observation logged 2026-06-08 (top-5 = `[BTC-USD, ETH-USD, ZEC-USD, XRP-USD, SOL-USD]`); full closure pending ~7d runtime-log accumulation post-prod; the bet is shippable without Q1 closure because the operator's `selected_assets` are persisted on the strategy row regardless of top-5 churn. | — | `/measure CB-3` cron picks up + Researcher Q1 final closure after ~7d log accumulation; CB-4 unblocked | 2026-06-08 actual | **2026-06-09 actual (20 days ahead of estimate)** |
| [CB-4](bets/CB-4/brief.md) | **CB-4.0 + CB-4.1 SHIPPED 2026-06-11** via [PR #58](https://github.com/vivekschaudhary/crypto-bot/pull/58) (`lib/signals/` Wilder's RSI + MA; +39 net tests; surfaced `[silent-optimization-vs-explicit-contract]` named anti-pattern) + [PR #60](https://github.com/vivekschaudhary/crypto-bot/pull/60) (`lib/decisions/` pure decision engine composes RSI/MA + Strategy + session totals into `{decision, reason, sizing}[]`; 6 Engineer DRI Decisions; +35 net tests after 2 BLOCKER closes — round-1 AC 8 sell-signal-no-position branch + relative-import boundary check; round-2 spec-honest reason amendment surfaced `[implementation-spec-gap-undetected-by-own-tests]` named anti-pattern). 2 of 4 stories shipped. **2 NEW PM DRI DECISIONS logged for forward stories** (via this PR's brief amendment): Decision #7 pins `getAccountTradeHistory` as the CB-4.2 cost-basis source (uniform dry-run + LIVE_MODE; operator's REAL portfolio drives bot's view from day 1); Decision #8 pins ALL transactions written to `orders` ledger from CB-4.3+ regardless of mode (dry_run + live unified for CB-5's audit dashboard + would-have-PnL computation). **CB-4.2 SHIPPED + VERIFIED 2026-06-13** ([PR #63](https://github.com/vivekschaudhary/crypto-bot/pull/63)) — cron tick handler live; migration 0005 reshaped `signals` to per-asset decision rows. **Production verification PASSED: 84 ticks / 0 errors / 100% tick reliability over ~21h**; real top-5 portfolio; dry-run; the CB-3-retro production-only-defect class did NOT recur. **CB-4 BET COMPLETE 2026-06-14 — all 4 stories shipped; CB-4.4 DROPPED.** CB-4.0/4.1/4.2/4.3 shipped (PRs #58/#60/#63/#65); the bot runtime + `*/15` cron tick + signal eval (`lib/signals/`) + decision engine (`lib/decisions/`) + dry-run/live order placement + unified `orders` ledger are all in production (shipping DARK at `LIVE_MODE=false`). CB-4.2 prod-verified (84 ticks/0 errors/100% reliability/21h); migrations 0005 + 0006 applied. **CB-4.4 (take-profit polish) DROPPED** — sell logic verified correct in prod since CB-4.2; no refinement needed. `key_metric` (≥99% tick reliability) GREEN (100% observed). **Remaining is operator-side only:** (1) the dry_run ledger's first row awaits a natural buy/sell signal crossing (market all-hold since deploy — write path deployed + tested, not yet exercised); (2) the `LIVE_MODE=true` flip is the deliberate operator ceremony behind the ≥60-dry-run-session guardrail (clock from 2026-06-12 21:45 UTC). | — | CB-4 closed. `/measure CB-4` cron picks up tick-reliability over the rolling window. | 2026-06-09 actual (CB-4.0 shipped) | **2026-06-14 actual (bet complete; 4 stories shipped, CB-4.4 dropped)** |
| [CB-6](bets/CB-6/brief.md) | **Brief APPROVED 2026-06-16 — FIRST POST-MVP BET.** Multi-asset shell + crypto **cockpit** redesign per the operator design (`ETH_USD Bot — Coinbase.pdf`): a **3-tab shell** (📊 Mutual Funds · 📈 Equity · 🤖 Crypto) + a single-screen crypto cockpit (status + Start/Pause/Stop/Run-now · current-run P&L · current value + position · live price + signals/next-action · manual overrides incl. **real-money** Buy/Sell 50%/Sell All · trade log w/ SKIPPED + reasons + filter). Recomposes CB-5's read models + the CB-5.3 multi-row session. **3 load-bearing decisions RESOLVED (operator):** (1) **per-pair VIEW** of the existing multi-asset bot (no CB-4/CB-3 model change; pair selector); (2) real-money overrides **built now, paper-while-dark** (un-defers CB-5.4; **inverts CB-5.3's `/api/bot/**` no-orders invariant** → mandatory Security Reviewer); (3) **Stop = alias for `paused`** (no migration). `architecture_required: auto` → recommend story-level + Security Reviewer (skip a separate bet-arch doc; patterns exist: CB-4.3 placeOrder + CB-5.3 route + override_events kinds already in schema). **Slice:** equity (Zerodha/India, own strategy) = **CB-7** (`architecture_required: true`, product.md amendment due there); Mutual Funds = later. **CB-6.0 SHIPPED 2026-06-16** ([PR #84](https://github.com/vivekschaudhary/crypto-bot/pull/84)) — 3-tab shell (MF/Equity/Crypto; MF+Equity placeholders) + `/dashboard` redesigned into the single-screen crypto cockpit scaffold + **Bot Status section** (Start/Pause/Stop via the reused CB-5.3 `/api/bot/override`; Stop=alias for paused, no migration; Run-now disabled→CB-6.3). Reuses CB-5.0 `loadLiveState`; no new backend, no migration; `/dashboard` confirmed dynamic (no static-prerender). +24 unit tests (797 total) + Codex cockpit/onboarding e2e; Codex-clean. **1 of ~5 CB-6 stories shipped.** **CB-6.1 SHIPPED 2026-06-17** ([PR #87](https://github.com/vivekschaudhary/crypto-bot/pull/87)) — SECOND CB-6 STORY (Codex-clean + e2e); **2 of ~6 CB-6 stories shipped** — **per-pair selector** (cockpit becomes a per-pair VIEW; `?pair=` SSR; per-pair title "ETH/USD Trading Bot") + **Current Position card** (held qty + avg cost via `aggregatePosition`, live price via `getProduct`, latest RSI from signals). Reuses CB-5.0/5.1/5.2 reads; no new backend, no migration. UI story (design.md + copy.md; `e2e: true`); 9 ACs. **Adaptive re-forecast** (P&L split out): CB-6.2 Profit/Loss (session-scoped invested/buys/realized — owns the session-vs-all-time modeling); CB-6.3 Signals+Next-Action; CB-6.4 Trade Log (SKIPPED+reasons); CB-6.5 Run-now; CB-6.6 real-money overrides (Buy/Sell — mandatory Security Reviewer). **CB-6.2 SHIPPED 2026-06-16** ([PR #90](https://github.com/vivekschaudhary/crypto-bot/pull/90)) — THIRD CB-6 STORY (Codex-clean after 3 BLOCKERs closed: gate P&L on active session, neutral zero, + the Codex Profit/Loss e2e); session-scoped invested/buys + all-time position P&L (resolved the session-vs-all-time scoping; reused computeAssetPnl + the loadPnl scoped-try/catch); **3 of ~6 CB-6 stories shipped**. **[CB-6.3 story](bets/CB-6/stories/CB-6.3/story.md) `ready` 2026-06-16 — FOURTH CB-6 STORY** (Signals + Next Action; DB-only recomposition of CB-5.1 signals — RSI zone strategy-relative not 30/70, single real MA per operator decision, Next Action = decision + reason verbatim; NOT session-gated — deliberate, differs from 6.2). **CB-6.3 SHIPPED 2026-06-16** ([PR #92](https://github.com/vivekschaudhary/crypto-bot/pull/92)) — FOURTH CB-6 STORY (Codex-clean first pass + Codex e2e); `loadCockpitSignals` (DB-only latest signal, not session-gated) + Signals card (strategy-relative RSI zone, single real MA, Next Action = decision+reason verbatim); **4 of ~6 CB-6 stories shipped**. **[CB-6.4 story](bets/CB-6/stories/CB-6.4/story.md) `ready` 2026-06-17 — FIFTH CB-6 STORY** (Trade Log: `orders⋈skips` merge — trades from the ledger + SKIPPED hold rows w/ reasons + status filter; read-only, no migration. **2 operator decisions:** Price/Qty deferred to post-flip (dry_run has no real fill); Reason on skip rows only — no `orders.tick_id` migration. All-time recent per pair, not session-scoped). **CB-6.4 SHIPPED 2026-06-17** ([PR #94](https://github.com/vivekschaudhary/crypto-bot/pull/94)) — FIFTH CB-6 STORY (Codex-clean after the AC-9 status-filter component-test BLOCKER was closed; Codex e2e committed + CI-typechecked, local e2e EXECUTION deferred under issue #80 / Docker test-DB availability, as for CB-6.0–6.3); `loadCockpitTradeLog` (two non-overlapping streams: orders + hold-signals, status filter scoped in SQL before the limit) + Trade Log card + `?txStatus=` filter; **5 of ~6 CB-6 stories shipped**. **[CB-6.5 story](bets/CB-6/stories/CB-6.5/story.md) `ready` 2026-06-17 — SIXTH CB-6 STORY** (Run Now: enable the disabled CB-6.0 button → operator-auth `POST /api/run-now` triggers ONE bot evaluation via an **extracted shared `runBotTick()`**, dry-run while dark, no LIVE_MODE bypass. **Architect decisions:** run-now lives OUTSIDE `/api/bot/**` so the CB-5.3 no-orders invariant stays green; manual tick uses a precise (non-floored) `tick_started_at` so it always runs fresh — resolves the dedupe open Q. `security_review: true` — new authenticated order-triggering endpoint). Then **CB-6.6 real-money overrides** (Buy/Sell — un-defers CB-5.4, inverts CB-5.3's no-orders invariant → **mandatory Security Reviewer**). **CB-6.5 SHIPPED 2026-06-17** ([PR #96](https://github.com/vivekschaudhary/crypto-bot/pull/96)) — SIXTH CB-6 STORY (Codex-clean after the AC-9 Run Now render-coverage BLOCKER + **Security Reviewer pass**; Codex e2e committed/CI-typechecked, local exec deferred under #80); extracted shared `runBotTick()` (cron + run-now), `POST /api/run-now` outside `/api/bot/**`, manual precise timestamp, Run Now button live; **6 CB-6 stories shipped**. **LAST: CB-6.6 real-money overrides** (Buy/Sell — un-defers CB-5.4, inverts CB-5.3 no-orders invariant → mandatory Security Reviewer). **[CB-6.6 story](bets/CB-6/stories/CB-6.6/story.md) `ready` 2026-06-17 — FINAL CB-6 STORY** (Manual Overrides: real-money Buy $<position_size_usd> / Sell 50% / Sell All / Reset, paper-while-dark. **Un-defers CB-5.4 + INVERTS the CB-5.3 /api/bot/** no-orders invariant** → behavioral tests replace the structural guard; `security_review: mandatory`. **Operator decisions:** manual buys count toward + are blocked by the per-session caps (hard ceiling, bot+manual); Buy size = strategy position_size_usd. No migration.). **CB-6.6 SHIPPED 2026-06-18** ([PR #98](https://github.com/vivekschaudhary/crypto-bot/pull/98)) — FINAL CB-6 STORY (Codex-clean after 3 BLOCKERs [render coverage, projected cap, idempotency] + **MANDATORY Security Reviewer pass** + Codex e2e); real-money Manual Overrides (Buy/Sell/Reset, paper-while-dark) via lib/bot/manual-orders.ts; INVERTED the CB-5.3 /api/bot/** no-orders invariant (+ structural guard that the safe overrides module stays order-free + behavioral LIVE_MODE-gate tests); projected cap ceiling (bot+manual); client idempotency key. **CB-6 BET SHIPPED — all 7 stories (6.0–6.6) done.** Next: operator-side (LIVE_MODE flip ceremony; CB-7 equity/Zerodha; issue #80 local e2e). **CB-6.7 SHIPPED 2026-06-18** ([PR #100](https://github.com/vivekschaudhary/crypto-bot/pull/100)) — POST-SHIP FIX (Codex-clean + Security glance + e2e): paper-aware cockpit P&L + Current Position (mode-switched: paper-while-dark from the dry_run ledger via synthesizePaperFills+computeAssetPnl, real post-flip; migration 0008 orders.base_quantity; Paper badge). Fixed the live "$400 invested / $0 value" (paper invested vs real position). **CB-6 BET RE-SHIPPED — 8 stories (6.0–6.7).** ⚠ apply migration 0008 to prod + Reset Session. | — | CB-6 closed (re-shipped). Apply migration 0008; /scan CB-6 (Production-Ready findings open). | 2026-06-16 (approved) | **2026-06-18 actual (8 stories)** |
| [CB-5](bets/CB-5/brief.md) | **Brief APPROVED 2026-06-14** (promoted PR #66 + HITL-approved at the MVP-transition milestone; `status: approved`). **LAST MVP BET — ready for `/create-story CB-5` (CB-5.0).** Transaction ledger + bot dashboard + safe override controls — the last MVP bet; closes product.md's "log all transactions" + "full decision-trace observability" clauses. Promoted IN PARALLEL with the CB-4 tail (dependency satisfied — `bot_ticks`/`signals`/`orders` shipped + verified in prod). 6 PM DRI Decisions (safe-controls-first — real-money force-buy/sell-N DEFERRED post-live-flip; SSR-per-load no polling; reuse the cost-basis read model; inline-styles consistency; parallel_with CB-4; holdings from `aggregatePosition` since `account_snapshots` unpopulated). 3 PM Risks; 1 Researcher Q (per-asset PnL scope). 4 MVP stories forecast (CB-5.0 live-state + read model; CB-5.1 decision-trace log; CB-5.2 ledger; CB-5.3 safe override controls) + deferred CB-5.4 (real-money overrides). Estimate refined 3wk/low → 1wk/medium. `architecture_required: false`. **CB-5.0 SHIPPED 2026-06-14** ([PR #69](https://github.com/vivekschaudhary/crypto-bot/pull/69)) — dashboard read model + live-state view on `/dashboard` + shared LIVE_MODE banner (both states Playwright-asserted via 2nd webServer); +10 tests; Codex-clean first pass. **CB-5.1 SHIPPED 2026-06-14** ([PR #71](https://github.com/vivekschaudhary/crypto-bot/pull/71)) — decision-trace view at `/dashboard/trace` (`bot_ticks` ⋈ `signals`, reasons verbatim; force-dynamic SSR — caught a static-prerender bug in the build); +7 tests; brief PM Decision #7 amended upstream (dry-run badge → ledger). **CB-5.2 SHIPPED 2026-06-14** ([PR #73](https://github.com/vivekschaudhary/crypto-bot/pull/73)) — transaction ledger at `/dashboard/ledger` + per-asset PnL (realized + unrealized; `replayFills` extracted from aggregatePosition — shared weighted-average basis; +30 tests); Researcher Q1 closed (PnL in MVP). The full dashboard READ surface (live-state + decision-trace + ledger) is now live. **CB-5.3 SHIPPED 2026-06-14** ([PR #75](https://github.com/vivekschaudhary/crypto-bot/pull/75)) — safe override controls (pause/resume/reset) via the new operator-auth `/api/bot/override` route + status-aware dashboard buttons; writes `bot_sessions.status` + `override_events` (CB-5's ONLY write surface). Reset = MULTI-ROW per the architecture (ends current session + starts new, history preserved). **Round-1 review: 2 Reviewer BLOCKERs + 1 Security MEDIUM, all closed** — session-integrity on the override write path (stale/concurrent fork-or-reopen) fixed via in-tx lock + `ended_at IS NULL` current-definition + **migration 0007** unique-index backstop; AC 11 e2e authored by Codex. Real-money overrides (force_buy/sell_N) deferred to CB-5.4 (post-flip; rejected 400). **CB-5 BET COMPLETE 2026-06-14 — all 4 MVP stories shipped (PRs #69/#71/#73/#75) → MVP COMPLETE. All 5 MVP bets (CB-1..CB-5) shipped.** The operator loop is closed: author → paper-trade → review → control. ⚠️ **Migration 0007 must be applied to prod** (like 0005/0006). Next: `/scan CB-5` (Build → Production Ready); the `LIVE_MODE=true` flip is operator ceremony (≥60-dry-run-session guardrail). | — | CB-5 closed. Apply migration 0007; `/scan CB-5`; `/measure CB-5`. | 2026-06-14 (approved) | **2026-06-14 actual (bet complete; 4 stories shipped; ~1 day vs 1wk est)** |

## Awaiting human approval

_None._

---

_**🎉 MVP COMPLETE 2026-06-14 — all 5 MVP bets (CB-1..CB-5) SHIPPED.** CB-5 (dashboard + safe override controls) shipped all 4 MVP stories (CB-5.0–5.3, PRs #69/#71/#73/#75). The operator loop is closed end-to-end: author strategy → paper-trade (cron) → review (live-state + decision-trace + ledger + PnL) → control (pause/resume/reset). **Operator-side next steps (no further build gate):** (1) ⚠️ apply **migration 0007** (`bot_sessions_single_current`) to prod, like 0005/0006; (2) `/scan CB-5` (Build → Production Ready phase boundary); (3) the `LIVE_MODE=true` flip remains operator-only ceremony behind the ≥60-dry-run-session guardrail (clock from 2026-06-12 21:45 UTC); (4) CB-5.4 (real-money force-buy/sell-N) is the deferred post-flip follow-up, NOT part of the MVP set._

_CB-4 status: **CB-4.0/4.1/4.2/4.3 all SHIPPED** (CB-4.3 via PR #65 merged 2026-06-14; migration 0006 applied; ships dark, LIVE_MODE=false). CB-4 tail in progress on a parallel track: dry_run ledger verification (after the next tick) + the CB-4.4 take-profit-polish drop/fold call (leans DROP). **The `LIVE_MODE=true` flip is operator-only ceremony, gated behind the ≥60-dry-run-session product guardrail — clock started 2026-06-12 21:45 UTC.**_

## Recently shipped

- **2026-06-14** — **🎉 CB-5 BET SHIPPED → MVP COMPLETE.** The operator dashboard + safe override controls — the last MVP bet. All 4 MVP stories shipped same-day: CB-5.0 live-state ([#69](https://github.com/vivekschaudhary/crypto-bot/pull/69)) + CB-5.1 decision-trace ([#71](https://github.com/vivekschaudhary/crypto-bot/pull/71)) + CB-5.2 ledger + PnL ([#73](https://github.com/vivekschaudhary/crypto-bot/pull/73)) + **CB-5.3 safe override controls ([#75](https://github.com/vivekschaudhary/crypto-bot/pull/75))**. **All 5 MVP bets (CB-1..CB-5) now shipped** — the operator loop is closed end-to-end (author → paper-trade → review → control). **CB-5.3 highlights**: CB-5's only write surface — `/api/bot/override` (operator-auth: rate-limit + CSRF + `verifySession` re-verify, mirroring CB-1.5) drives pause/resume/reset; reset is MULTI-ROW per the architecture (ends current session + starts new, history preserved). **Round-1 review: 2 Reviewer BLOCKERs + 1 Security MEDIUM, all closed** — the substantive one was session-integrity on the write path (a pre-loaded session id mutated in a later transaction, "current" defined by `started_at` alone → stale/concurrent fork-or-reopen); fixed by resolving + locking the current session in-transaction, redefining "current" as `ended_at IS NULL` across all reads, and **migration 0007** (`bot_sessions_single_current` partial unique index) as a structural backstop (forked insert → 23505 → 409). AC 11 e2e authored by Codex (Phase 3). +47 net tests (782 unit). ⚠️ **Migration 0007 must be applied to prod.** Next: `/scan CB-5` + the operator-ceremony `LIVE_MODE` flip.
- **2026-06-13** — **CB-4.2 build shipped + VERIFIED IN PRODUCTION via [PR #63](https://github.com/vivekschaudhary/crypto-bot/pull/63) (squash merge `e535d7f`).** Cron tick handler replaces the heartbeat stub — the bot's first real decision loop. Migration 0005 reshaped `signals` to per-asset decision rows (executed the architecture `Signal` amendment). **Post-deploy verification PASSED** (story PM Risk #1 — first live cron exercise, production-only-defect class on explicit watch): queried production Postgres — **84 ticks, 420 signal rows (exactly 5/tick), 0 errors, 100% tick reliability over ~21h** (first row 2026-06-12 21:45 UTC; all `tick_started_at` on clean :00/:15/:30/:45 boundaries; ~2.4s/tick vs the 60s ceiling; rate-limit headroom 29/30). Decisions ran the real signal math against the operator's real top-5 portfolio (BTC/ETH/ZEC/XRP/SOL) — including a live ETH `position open` via cost-basis aggregation (Decision #7 end-to-end) and the CB-4.1 round-2 amended `exit rsi condition met … but no open position` reason on SOL. **The CB-3-retro production-only-defect class did NOT recur.** Review history: 1 round (2 BLOCKERs — session-pointer strategy resolution + invocation-anchored error-window; 1 security medium — error-detail sanitization) + 1 CI round (gitleaks on a JWT-shaped test fixture → assembled-at-runtime fix) + 1 history-cleanup round (squashed to drop the flagged literal from the commit range). +45 net tests (628 → 673). **Squash-race streak: 19 PRs clean (#45–#63).** The ≥60-dry-run-session guardrail clock for the `LIVE_MODE` flip starts here.
- **2026-06-11** — **CB-4.1 build shipped via [PR #60](https://github.com/vivekschaudhary/crypto-bot/pull/60) (squash merge `3154b61`).** `lib/decisions/` pure decision engine — SECOND CB-4 STORY. 3 source files (types.ts, evaluate.ts, index.ts) + 3 test files (evaluate.test.ts, no-coupling.test.ts, no-live-mode.test.ts) + 6 Engineer DRI Decisions in evaluate.ts JSDoc. **Test count: +35 net at merge** (suite 593 → 628 with the 2 round-1 BLOCKER regression tests included). **Headline outcomes**: (a) `evaluate(strategy, perAssetSignals, sessionTotals): DecisionResult[]` composes RSI/MA values from CB-4.0 + Strategy from CB-3.0 + session totals into deterministic decisions per asset, in input order. Reason strings (operator-readable plain text with stable named prefixes per decision class) ARE the operator-audit contract surfaced to bot_ticks.reason. (b) **NEW NAMED ANTI-PATTERN surfaced via Codex round-1**: **`[implementation-spec-gap-undetected-by-own-tests]`** — round-1 was missing the AC 8 "no-position + sell-signal" branch; round-2 caught the related contract-honesty gap (story.md AC 8 was structurally impossible as written — exit rule has 2 conditions but profit_pct can't be computed without a position). Fix was a cross-artifact sweep amending BOTH story.md AC 8 and the implementation reason string to spec-honest form. This is a NEW VARIANT of `[cross-artifact-sweep-on-contract-shift]` — **latent drift**: both sides quietly wrong from the start, not just one drifting from another. (c) 2 architectural invariant tests with the strategy-core ALLOW-LIST (extends CB-4.0's pattern — decisions allows `@/lib/strategy-core/*` because Strategy types ARE the contract per CB-3 PM Decision #6). Codex code + security reviews clean. **Squash-merge race streak extended to 16 PRs clean (#45 through #60)**.
- **2026-06-09** — **CB-4.0 build shipped via [PR #58](https://github.com/vivekschaudhary/crypto-bot/pull/58) (squash merge `a7a61c6`).** `lib/signals/` foundation — pure Wilder's-smoothing RSI + simple MA calculators; FIRST CB-4 STORY. 3 source files (rsi.ts, ma.ts, index.ts) + 4 test files (rsi.test.ts, ma.test.ts, no-coupling.test.ts, no-live-mode.test.ts) + 6 Engineer DRI Decisions in source JSDoc. **Test count: +39 net at merge** (suite 554 → 593) — the first commit added +37 (initial implementation) and the round-1 BLOCKER fix added +2 regression tests pinning the AC 6 full-array NaN contract. **Headline outcomes**: (a) Researcher Q1 (candle granularity = ONE_HOUR) + Q2 (look-back = 65 bars) both CLOSED at first build commit via Engineer DRI Decisions. (b) Two pre-PR self-caught findings: the "RSI converges to 50 under flat extension" prose claim was mathematically wrong (Wilder ratio invariance preserves RSI when both avg_gain + avg_loss decay by same factor); fix preserved correct math + reframed test claim. (c) **NEW NAMED ANTI-PATTERN surfaced via Codex round-1**: **`[silent-optimization-vs-explicit-contract]`** — my `ma.ts` window-only NaN check optimized away the AC 6 contract ("NaN in input array → null", full stop). 591 passing tests didn't catch it because the contract was under-tested. The fix scanned the FULL input array + added regression tests pinning `ma(3, [NaN, 1, 2, 3]) → null`. Worth folding into AGENTS.md alongside the 4 existing named patterns from the CB-3 retro. Codex code + security reviews clean. **Squash-merge race streak extended to 14 PRs clean (#45 through #58)** — pre-merge `headRefOid` ritual continues to hold.
- **2026-06-09** — **CB-4 brief APPROVED** via [PR #56](https://github.com/vivekschaudhary/crypto-bot/pull/56) (HITL milestone gate fired; operator-typed `status: proposed → approved` directly on the brief frontmatter; Codex review clean; 1-file 4-line PR). Unblocks `/create-story CB-4` for CB-4.0.
- **2026-06-09** — **CB-4 brief PROMOTED** from portfolio stub via `/create-brief CB-4`. Full brief content: problem (operator has typed strategies via CB-3 but no autonomous execution); hypothesis (cron `*/15` → read active strategy → fetch candles via CB-2 → compute RSI/MA in `lib/signals/` → evaluate via `lib/decisions/` → write `bot_ticks` + `signals` → conditionally `placeOrder` gated by `LIVE_MODE`); primary metric (bot tick reliability ≥ 99% over 30d window — matches foundation architecture fitness function); 5 guardrails (no LIVE_MODE order in dry-run; tick uniqueness; per-session cap enforcement; append-only audit; Coinbase rate-limit budget); 6 PM DRI Decisions (defer overrides to CB-5; limit-only orders; single-tier exit rules; LIVE_MODE env-var-only; cron respects `bot_sessions.status='paused'`; 1wk medium estimate); 5 PM Risks (signal computation error; LIVE_MODE accidental flip; cron overlap; Coinbase rate-limit; take-profit semantics drift); 3 Researcher Open Questions (candle granularity; look-back window; limit-order slippage — closures forecast at /build CB-4.0 + .3); 4-5 stories forecast (CB-4.0 `lib/signals/`; CB-4.1 `lib/decisions/`; CB-4.2 cron handler dry-run; CB-4.3 `LIVE_MODE` gate; CB-4.4 maybe take-profit polish). `architecture_required: false`. Bundled portfolio.md backfill (CB-3 row was missing from Promotion log table — added during cross-artifact sweep per the retro pattern). Awaiting HITL approval before `/create-story CB-4`.
- **2026-06-09** — **CB-3 BET SHIPPED.** All 4 stories (CB-3.0 + CB-3.1 + CB-3.2 + CB-3.3 after CB-3.4 fold) + 4 post-merge polish PRs (#50/#51/#52/#53). **1.5 calendar days end-to-end vs 3 wk medium-confidence brief-approval estimate (~17x faster)**. Strategy authoring + persistence + activation surface live in production at `/dashboard/strategy`. **9-PR squash-race streak clean (#45 + #46 + #47 + #48 + #49 + #50 + #51 + #52 + #53)** — pre-merge `headRefOid` ritual held flawlessly through the entire bet. **3 production-only defects** surfaced operator-side within hours of CB-3.3 going live; all fixed forward without reopening the story formally: (a) **`[rsc-prop-serialization]`** (PR #50) — passing `adapter: AssetAdapter` from Server Component to Client Component caused 3 `E{digest:...}` broken-reference errors in production; React Server Components can only serialize Server Actions across the RSC boundary, not regular function methods. Local `pnpm dev` + `pnpm build` too lenient. Fix: replace adapter prop with primitives (`assetClass` + resolved `candidates[]`). (b) **`[server-action-file-export-purity]`** (PR #51) — `"use server"` files can only export async functions; my dead re-export `export { VALIDATION_ERROR_CODES, errorCodeToFieldPath }` violated the rule. Production-only error: `A "use server" file can only export async functions, found object`. (c) **`[empty-numeric-input-zero-trap]`** (PR #52) — clearing a number input set state to `0` (because `Number("") === 0`) leaving the field showing "0" with no way to visually clear. Operator-reported. Fix: NaN sentinel + Number.isFinite gate on submit. **Bonus PR #53** lifted the 3 remaining crypto-flavored copy strings ("1-5 cryptos", "Selected from top-5 by dollar volume", "Pick between 1 and 5 cryptos") to a route-layer `labels: StrategyFormLabels` prop so the form Client Component is genuinely asset-class-agnostic at the copy level too — equity-app extraction via `@vc1023/strategy-core` is now a literal half-day find/replace per [brief PM DRI Decision #6](bets/CB-3/brief.md#decisions). **Researcher Q2 (RSI period default) CLOSED** at `/create-story CB-3.3` (RSI period not exposed in form; CB-4 hardcodes RSI(14)). **Researcher Q1 (top-5 stability cadence) SEEDED, NOT YET CLOSED** — first empirical observation logged 2026-06-08 with `[BTC-USD, ETH-USD, ZEC-USD, XRP-USD, SOL-USD]`; **full closure pending ~7d runtime-log accumulation post-prod** (not blocking ship; operator's `selected_assets` are persisted on the strategy row regardless of top-5 churn). **Cross-artifact-drift caught twice** (PR #50 round-1 + PR #53 round-1) — Codex flagged each time that story.md AC 3 + architecture.md Decision #6 had to be amended in the same PR as the code change. Final test count: 554 passing | 17 skipped. CB-4 (bot runtime + `*/15` cron tick) unblocks. The 3 production-only-defect lessons + the cross-artifact-drift recurrence both worth surfacing in a future `/retro`.
- **2026-06-09** — **CB-3.3 build shipped via [PR #49](https://github.com/vivekschaudhary/crypto-bot/pull/49) (squash merge `0a4c864`).** FOURTH + LAST CB-3 STORY (after CB-3.4 fold). 15 ACs; Standard Experience Checklist 5/6 load-bearing; `/dashboard/strategy` Server Component shell + Client Component generic form + `saveStrategy` server action that validates + INSERTs + supersession + UPSERTs `bot_sessions.active_strategy_id` + structured-JSON log + redirect. First Playwright e2e since CB-1.6 (2 specs — happy-path persistence roundtrip + supersession-FK roundtrip). 8 Engineer DRI Decisions including the `bot_sessions`-no-`user_id` singleton-via-SELECT-then-INSERT-OR-UPDATE deviation (documented inline). 9 ACs → 15 final after Codex code review surfaced 4 BLOCKERs (Playwright e2e missing → Engineer shipped despite Phase-3 ambiguity; in-action verifySession re-verify required; StrategyFormPayloadSchema defense-in-depth; top-5 timeout degradation when first-time authoring) + 1 ISSUE (submit disabled while invalid) + 1 Security MEDIUM (same root as BLOCKER 2; closed by same fix). Round 2 closed a contract gap (browser title metadata per copy.md). **All within ~3 review rounds**. 539 → 541 unit tests + 2 Playwright specs at merge. Codex security review clean. Squash-race streak: 5 PRs clean in a row (#45/#46/#47/#48/#49).
- **2026-06-08** — **CB-3.2 build shipped via [PR #48](https://github.com/vivekschaudhary/crypto-bot/pull/48) (squash merge `1b86615`).** `db/migrations/0004-strategies.sql` — creates `strategies` table verbatim per [bet architecture Decision #4](bets/CB-3/architecture.md) (ULID `text` PK; `asset_class` discriminator; `selected_assets`/`entry_rules`/`exit_rules` jsonb; 3 numeric CHECK constraints; FK to `auth_users`; self-FK supersession) + `ALTER TABLE bot_sessions ADD COLUMN active_strategy_id text REFERENCES strategies(id)`. `db/schema.sql` canonical reference updated (strategies in product entities section; cross-section FK constraints via idempotent DO blocks after auth section). **Reuses CB-3.0 Zod types directly** — no duplicate type defs; `tests/lib/strategy-core/db-roundtrip.test.ts` (6 tests) enforces via DB-row fixtures → `StrategySchema.parse()` + full JSON roundtrip (parse → stringify → re-parse → deep equal); `tests/db/migration-0004-constraint-alignment.test.ts` (4 tests) closes PM Risk #2 (DB CHECK ↔ Zod drift over time). **Architecture Issue #1 RESOLVED at story drafting** (not at build) — `bot_sessions` already exists in `0001-init.sql:23-30` → CB-3.2 only adds the FK column. **1 review round** — round-1 BLOCKER (schema.sql section ordering — moved strategies into product entities section + cross-section FK ALTERs after auth) + ISSUE (db-roundtrip test claim mismatch → full JSON cycle assertion added). Codex security review clean. **Squash-merge race streak extended to 4 PRs clean (#45 + #46 + #47 + #48)**. Vercel auto-migrate applies 0004 on next production deploy.
- **2026-06-08** — **CB-3.1 build shipped via [PR #47](https://github.com/vivekschaudhary/crypto-bot/pull/47) (squash merge `ebd34ca`).** `lib/strategy-coinbase/adapter.ts` — FIRST REAL `AssetAdapter` implementation; ~120 lines + 6 Engineer DRI Decisions in JSDoc; the SINGLE file in CB-3 where crypto-coinbase coupling lives. **Headline outcome — empirical /build discovery**: ranking field switched from `volume_24h` (base-currency TOKEN count; biased to meme coins) to **`approximate_quote_24h_volume`** (Coinbase pre-computed DOLLAR volume; per-product field). Live integration test confirmed top-5 by dollar volume = `[BTC-USD, ETH-USD, ZEC-USD, XRP-USD, SOL-USD]` (BTC ranks #1 of 114 USD-quoted spot products). Concurrent cross-artifact sweep: CB-3 brief + architecture Decision #3 + CB-3.1 story + CB-2.2 `market-schemas.ts:84` all updated in the same PR — lesson encoded ("verify field semantics empirically before locking across artifacts"). 15 unit tests (filter + ranking + meme-coin-trap + tie-stability + stateless) + 3 live integration tests (double-gated) + 1 sibling no-LIVE_MODE invariant test. **1 review round**: round-1 BLOCKERs were AC 6 single-anchor → two-anchor (BTC + ETH); AC 5 stable-sort test missing → added (pins V8 ECMAScript 2019 stable sort); ISSUE — `market-schemas.ts:51` stale comment → rewritten. Codex security review clean. **Researcher Open Question #1 SEEDED** (first empirical observation logged); full closure happens after ~1 week of runtime-log accumulation post-CB-3.3. **Squash-merge race streak extended to 3 PRs clean (#45 + #46 + #47)**.
- **2026-06-08** — **CB-3.0 build shipped via [PR #45](https://github.com/vivekschaudhary/crypto-bot/pull/45) (squash merge `4a31bb7`).** `lib/strategy-core/` foundation — pluggability primitive; FIRST CB-3 STORY. 6 files (types, adapter interface, validate, supersession, top-n, form-schema) + 6 Engineer DRI Decisions + 53 unit tests + bundle 40K. **2 load-bearing architectural-invariant tests pass**: (a) transitive-walk no-coupling test forbids ANY `@/lib/...` import escaping strategy-core (strictly stronger than the original AC 7's coinbase/env/db check; future-proofs against new internal libs); (b) mock equity adapter test proves the `AssetAdapter` abstraction works for asset class OTHER than crypto-coinbase. **3 review rounds**: round-1 (2 BLOCKERs + 1 ISSUE: shape conventions; supersession contract; regex-only invariant → transitive walk); round-2 (2 BLOCKERs: convention split top-level snake_case + inner jsonb camelCase; `validateStrategyPayload` permissive input schema so named error codes actually fire per AC 3); round-3 (1 BLOCKER: `created_at: z.date() → z.coerce.date()` for clean JSON roundtrip per AC 1). Codex security review clean. **Squash-race streak restarted at 1 PR clean post-PR #43 restoration.**
- **2026-06-08** — **CB-2 BET SHIPPED.** All 5 stories (CB-2.1 + CB-2.2 + CB-2.3 + CB-2.4 + CB-2.5) shipped 2026-06-06 → 2026-06-08; **3 calendar days actual vs 1-week brief estimate**. Typed Coinbase Advanced Trade wrapper complete: `lib/coinbase/{client, jwt, types, market, accounts, orders, trace}.ts` + Zod schemas + 395+ tests + live integration coverage. Headline closures: EdDSA brokerage caveat RESOLVED (CB-2.3); real-money write path verified end-to-end (CB-2.4); rate-limit headers empirically discovered (CB-2.5 — auth'd brokerage returns `x-ratelimit-*` with 30 RPS global limit; public market doesn't); CDP JWT rotation behavior closed via code-review (CB-2.5). All 3 Researcher Open Questions closed. Squash-merge race streak: 4 occurrences in early CB-2 (PR #28→#29, #27→#30, #34→#35, #37→#38) → 3 PRs clean in a row (#39 + #40 + #41) via pre-merge `headRefOid` verification ritual from the triage note. CB-3 (strategy authoring) unblocks; brief promoted same-day via `/create-brief CB-3`.
- **2026-06-08** — **CB-3 BRIEF PROMOTED + APPROVED + PIVOTED TO PLUGGABLE ARCHITECTURE** (from portfolio stub to full brief + same-session amendment + bet-architecture artifact). Strategy authoring + top-5 selection ships as `lib/strategy-core/` (Coinbase-free; portable; designed for npm extraction) + `lib/strategy-coinbase/` adapter — per the `@vc1023/passkey-2fa` precedent. **5 PM DRI Decisions** including #6 (pluggable architecture); **4 PM Risks** including pluggable-scope-cost-vs-equity-app-shipping; **5 stories forecast** (CB-3.0 strategy-core foundation → CB-3.1 coinbase adapter → CB-3.2 DB schema → CB-3.3 form UI + Playwright → CB-3.4 activation); estimate refined 2 wk → 3 wk medium-confidence brief-approval; `architecture_required: false → true` with bet-architecture artifact at `docs/bets/CB-3/architecture.md` (Codex PR #42 round-1 BLOCKER on widening architecture scope inside brief). Forward-reference e2e expectation called out — CB-3.3 form UI is first Playwright re-engagement since CB-1.6. Bundled with CB-2's bet-shipped cascade-sync.
- **2026-06-08** — **CB-2.5 build shipped via [PR #41](https://github.com/vivekschaudhary/crypto-bot/pull/41) (squash merge `e343f7e`).** Observability layer + rate-limit awareness — LAST CB-2 STORY. `lib/coinbase/trace.ts` (~150 lines): emits structured-JSON `console.log` per Coinbase request with `{method, path, status, duration_ms, rate_limit?}`. client.ts wired via try/catch/finally pipeline (both auth'd `request` + `publicRequest`). jwt.ts JSDoc updated with CDP JWT rotation Decision #5 inline closure. 15 unit tests + 3 live integration tests. **Headline outcome — Researcher Open Question #1 RESOLVED empirically**: auth'd brokerage endpoints DO return `x-ratelimit-limit`, `x-ratelimit-remaining`, `x-ratelimit-reset` with 30 RPS global limit (RFC 7234-ish format); public market endpoints do NOT. Operator's ~96 ticks/day at ~0.001 RPS is vastly under the 10%-of-ceiling guardrail; no back-off logic needed. **Squash-merge race streak extended to 3 PRs clean** (#39 + #40 + #41). Sentry SDK install deferred per operator (structured logs satisfy key_metric).
- **2026-06-08** — **CB-2.4 build shipped via [PR #39](https://github.com/vivekschaudhary/crypto-bot/pull/39) (squash merge `51196f2`).** Auth'd WRITES — first real-money write surface. `lib/coinbase/orders.ts` + `order-schemas.ts` — two typed wrappers (`placeOrder({productId, side, orderConfiguration, clientOrderId?})` with discriminated-union order_configuration + auto-generated UUID v4 idempotency key via `crypto.randomUUID()`; `cancelOrders({orderIds: string[]})` matching Coinbase's batch_cancel endpoint). 17 unit tests + 3 triple-gated integration tests (real orders via `RUN_REAL_ORDER_TESTS=1` opt-in). 6 Engineer DRI Decisions including success-gated nested envelope schema via `.superRefine()` (Codex round-1 BLOCKER). **LIVE_MODE-free architectural invariant** verified by existing grep test. **Real write path verified end-to-end**: limit BUY 50% below market with post_only=true accepted; cancel returned success; no fill. Codex code + security review both clean. **Live discovery**: Coinbase rejects far-from-market limits with `PREVIEW_LIMIT_PRICE_TOO_FAR_FROM_MARKET` (initial $1 BTC test triggered this; 50% below market is within tolerance). **Squash-merge race STREAK BROKEN** (4 prior occurrences in this bet; pre-merge `headRefOid` verification ritual worked).
- **2026-06-08** — **Triage note documented for squash-merge race-condition pattern** ([docs/incidents/2026-06-07-squash-merge-race/triage.md](incidents/2026-06-07-squash-merge-race/triage.md)) via [PR #36](https://github.com/vivekschaudhary/crypto-bot/pull/36) (commit `4e2fa0f`). P3 process hygiene. Captures the recurring pattern that bit 3 times in CB-2 (PR #28→#29, PR #27→#30, PR #34→#35): GitHub squash-merge UI captures branch HEAD AT click-time; subsequent commits to the same branch are NOT auto-included. Worst observed outcome MEDIUM (PR #35 silent match-all on financial data); worst possible HIGH (PR #29 fail-open security gate). Proposed mitigation: verify `gh pr view <N> --json headRefOid` matches local before clicking merge. If a 4th occurrence happens → escalate to P2 + add hard gate (custom GitHub Action).
- **2026-06-07** — **CB-2.3 build shipped via [PR #34](https://github.com/vivekschaudhary/crypto-bot/pull/34) (squash merge `d642b99`) + [PR #35](https://github.com/vivekschaudhary/crypto-bot/pull/35) restore (squash merge `925a187`).** Authenticated-reads layer of the Coinbase wrapper. `lib/coinbase/accounts.ts` + `account-schemas.ts` — three typed wrappers (`getAccountBalances()` auto-paginated with `has_next`+`cursor`, `getAccount(uuid)` with `{account: ...}` envelope unwrap, `getAccountTradeHistory({productIds[], ...})` single-page with caller-driven cursor) consuming `request()` from CB-2.1; first JWT brokerage path exercised end-to-end. 17 unit tests + 3 gated integration tests against real Coinbase auth'd endpoints (CDP creds required via `pnpm test:integration` new script). 8 Engineer DRI Decisions including the headline: **EdDSA brokerage caveat = `pass`** — CB-2.1 PM Risk RESOLVED with operator's actual key format. Codex round-1 BLOCKER: singular `productId` → plural `product_ids` array per docs (silent-match-all risk; restoration in PR #35 also added sentinel-empty-filter test pattern); fail-loud `pagination-contract-violation` error code replaced silent partial-list defensive break. Codex security review clean. Unexpected discovery: `GET /accounts/{uuid}` wraps in `{account: ...}` envelope (unlike `getProduct(id)` which doesn't).
- **2026-06-07** — **CB-2.2 build shipped via [PR #32](https://github.com/vivekschaudhary/crypto-bot/pull/32) (squash merge `c86991a`).** Public market data layer of the Coinbase wrapper. `lib/coinbase/market.ts` + `market-schemas.ts` — three typed wrappers (`getProducts()` auto-paginated, `getProduct(productId)`, `getProductCandles({...})`) consuming `publicRequest()` from CB-2.1; no JWT path exercised. 15 new unit tests + 3 gated integration tests against real Coinbase public endpoints; 346 total tests. 5 Engineer DRI Decisions: auto-paginate at limit=250, Unix-seconds time format, 9 granularity enum values, Zod failures → `CoinbaseClientError({code: "validation-failed"})`, fail-loud on > 350-candle range. Codex round-1 BLOCKER: FOUR_HOUR granularity + 350-candle max + `volume_24h` required (lesson: verify Coinbase contract values against current docs BEFORE writing schemas, not after Codex flags drift). Round-2 ISSUE: stale narrative comments still naming 300; round-3 clean.
- **2026-06-07** — **Vercel auto-migrate pipeline verified end-to-end.** First production deploy after PR #32 merge exercised `pnpm db:migrate && next build` in the Vercel build container against the operator's production DB (no-op — all 3 migrations already tracked). Preview deploy verified to SKIP the migration step (fail-closed gate via `MIGRATE_DESTINATION=production` env var set in Production scope ONLY). Operator-side env-var setup: `.env.local` has `MIGRATE_DESTINATION=production`; Vercel Production scope has the same; Preview scope does NOT (verified by inspecting the latest preview build log showing the explicit skip message).
- **2026-06-06** — **Migration 0003 (RLS on `auth_*` tables) applied to production Supabase.** Operator ran `pnpm db:migrate` after baseline-seeding the `_migrations` tracking table in Supabase SQL Editor — needed because `0001-init.sql` + `0002-auth-users-singleton.sql` landed manually during initial scaffold setup (before the migrate runner existed), so the runner's tracking table had no record of them; without the seed, the runner would re-attempt `0001-init.sql` against an already-populated schema and fail on `CREATE TABLE assets` (no `IF NOT EXISTS`). Verified with `SELECT tablename, rowsecurity FROM pg_tables WHERE tablename LIKE 'auth_%'` — all four `auth_*` rows return `rowsecurity = true`. Supabase dashboard's "Unrestricted" warning cleared. Closes the open operator action item from the 2026-06-06 / PR #22 follow-up trail (see line below). Follow-up `/ops` PR queued: wire `pnpm db:migrate` env-loading (`--env-file=.env.local`) + automate via Vercel build step so future migrations don't need manual local apply.
- **2026-06-06** — **CB-2.1 build shipped via [PR #26](https://github.com/vivekschaudhary/crypto-bot/pull/26) (squash merge `fc88b5f`).** Coinbase Advanced Trade wrapper — direct REST + JWT (no SDK). `lib/coinbase/client.ts` (lazy-cached singleton with `safeFetch` transport-failure wrap), `lib/coinbase/jwt.ts` (per-request JWT mint; ES256 for PEM EC keys + EdDSA for raw base64 Ed25519, auto-detected from key format), `lib/coinbase/types.ts` (`CoinbaseClientError` with `fromHttpResponse` factory). 46 new unit tests + 2 gated integration tests (public Coinbase market endpoints — no CDP creds required to run); 331 total tests pass. Codex 2 round-1 BLOCKERs closed (transport-failure wrap + EdDSA brokerage caveat per operator's option-b choice); round-2 + security review clean. Engineer DRI Decision: NO SDK (rejected `tiagosiebler/coinbase-api` and 2 others; rationale + alternatives logged). CB-2.1 closes [foundation arch DRI Issue #1](foundation/architecture.md#issues) — final SDK pick resolved as "no SDK, direct REST."
<!-- Note: a stale "CB-2.1 story drafted at status: ready" entry was previously here; removed as it represented a story-creation milestone (not a ship event) that has since been superseded by the actual CB-2.1 ship via PR #26 entry above. Story-creation events are captured in the plan refinement log (foundation/plan.md), not the status.md Recently shipped section, which is for shipped-only items per the PM role / template. -->

- **2026-06-06** — **[CB-2 brief](bets/CB-2/brief.md) approved** by operator (status `proposed` → `approved` same day as promotion). Brief was promoted earlier in the morning from portfolio stub via `/create-brief CB-2`; HITL approval landed same-session after the operator confirmed the 5 PM DRI Decisions hold. CB-2 is now the new critical-path entry per [plan v6](foundation/plan.md); CB-3 unblocks when CB-2 ships.
- **2026-06-06** — `/plan` v6 refresh shipped via [PR #23](https://github.com/vivekschaudhary/crypto-bot/pull/23) (squash merge `<sha>`). First post-CB-1-shipped refresh; CB-1 fires "All bet stories merged" trigger with `actual_end = 2026-06-05` (16 days ahead of v5's `estimated_end`). CB-2 becomes new binding-dep for CB-3 (CB-1 cleared); CB-2's `estimated_start` slipped 5 days (06-01 → 06-06) per pragmatic-derivation rule; MVP target stays 2026-08-16 (cascade math nets to zero). Codex round-1 BLOCKER (CB-1 estimate frontmatter mixed actuals with estimates; preserve estimate.* on bet-shipped) addressed in round-2; round-2 + round-3 clean. Two PM DRI Decisions superseded per Compass append-only convention.
- **2026-06-06** — Three follow-up PRs from the [2026-06-05 canary verification debug retro](retros/2026-06-05-canary-verification-debug-retro.md) all merged: **PR #20** (`/fix`) — `register/begin` HTTP 403→409 status correction + `setup-client.tsx` error mapping by response-body field not status alone + `classifyErrorResponse` helper extraction + 25 unit/integration tests. **PR #21** (`/ops`) — `runbook.md` "lost all passkeys + backup code" SQL completeness (`DELETE FROM auth_users` was missing) + `crypt-bot.kindtree.us` → `crypto-bot.kindtree.us` typo cleanup across 14 files (compass/config.yaml + brief + 10 test mocks). **PR #22** (`/ops`) — Supabase RLS hygiene migration 0003 + canonical `schema.sql` updates + runbook RLS posture documentation. All three Codex reviews + Codex security reviews clean. ~~**Operator action item open:** apply migration 0003 to production Supabase (not auto-applied per runbook pattern).~~ **CLOSED 2026-06-06** — see the migration-applied entry at the top of this section for the full closure detail.
- **2026-06-05** — **[CB-1](bets/CB-1/brief.md) bet SHIPPED.** The foundational auth bet — passkey-only authentication, single-operator, no third-party IdP in the auth path. **7 stories shipped in 5 calendar days** (CB-1.1, CB-1.1.1, CB-1.2, CB-1.3, CB-1.4, CB-1.5, CB-1.6) + 1 security follow-up PR. **2.3 weeks ahead of plan v5's 06-21 estimate.** Critical-path predecessor for CB-3..CB-5 now resolved; downstream bets unblocked. Variance attributable to: (a) n=1 means no stakeholder review-cycle friction, (b) multi-model A/B review discipline catching bugs same-cycle, (c) story decomposition tracking actual implementation complexity rather than padded fixed-week boundaries. Closes CB-1 guardrail #1 ("zero unauthenticated capital-touching requests") bidirectionally (CB-1.4 read-side proxy gate + CB-1.5 write-side row deletion). All four CB-1 brief PM-Decision questions resolved (single-device passkey scope, no separate bet architecture, sign-in success rate as primary metric, challenge-storage approach deferred-then-closed). Both PM-Issue open questions closed (challenge-storage approach by CB-1.2 Engineer DRI; first-deploy onboarding UX by CB-1.6). 4 PM Risks all mitigated and held under live testing.
- **2026-06-05** — Audit-driven security follow-up shipped via [PR #18](https://github.com/vivekschaudhary/crypto-bot/pull/18) (squash merge `4e6c7ea`). M1 (DB-pool exhaustion via uncached pre-auth Server-Component DB counts) closed via new `lib/auth/credential-count.ts` (`unstable_cache` + 60s TTL + tag invalidation in `register/finish`). M2 (`safe-next` allowlist drift between proxy.ts and lib/auth/safe-next.ts; false documented invariant) closed via consolidation — `lib/auth/safe-next.ts` is now single source of truth (stricter `includes("//")` rule); `proxy.ts` imports from there. **Closes CB-1.6 PM DRI Decision #3 deferral earlier than scheduled.** 4-round Codex review cycle; security review clean throughout. 260 Vitest tests + 5 E2E specs at merge.
- **2026-06-04** — Operator-requested codebase security audit shipped at [docs/retros/2026-06-04-codebase-security-audit.md](retros/2026-06-04-codebase-security-audit.md). Fresh-Agent Claude advisory audit; **0 CRITICAL · 0 HIGH · 2 MEDIUM (both fixed in PR #18) · 5 LOW.** Most notable positive: CB-1.5 sign-out endpoint identified as best-shaped route in the codebase — future CB-2+ handlers should pattern-match against it. Validates the supplemental codebase-audit pattern as complementary to per-PR Codex review (M2 was a cross-cutting invariant that per-PR review can't catch).
- **2026-06-04** — [CB-1.6](bets/CB-1/stories/CB-1.6/story.md) shipped via [PR #17](https://github.com/vivekschaudhary/crypto-bot/pull/17) (squash merge `9d26b8c`). First-deploy onboarding UX: 4 new/changed surfaces (`/` mode-detecting landing, `/setup`, `/sign-in`, minimal `/dashboard` with sign-out button) + `proxy.ts` redirect-target update + scaffold cleanup. **First UI-surface story in CB-1.** First story with Accessibility category NOT marked `n/a`. First mechanical assertion of the brief's `<5-min time-to-first-authenticated-dashboard` guardrail via Playwright AC 8 E2E. **Codex's AC 8 E2E surfaced 2 real production bugs** (`@simplewebauthn/browser@11` API drift + begin-response shape mismatch) that static mocks masked — clean replication of the `[mechanical-output-verification]` pattern from PR #10, one layer earlier (E2E vs production). 3-round Codex review cycle on PR #17; 6 BLOCKERs + 4 ISSUEs across rounds 1-2; round 3 clean. 246 Vitest + 5 E2E.
- **2026-06-02** — [CB-1.5](bets/CB-1/stories/CB-1.5/story.md) shipped via [PR #15](https://github.com/vivekschaudhary/crypto-bot/pull/15). Sign-out endpoint (`POST /api/auth/sign-out`) — verifySession + invalidateSession composition + cookie-clearing (Max-Age=0 with attribute parity vs issuance) + origin-check (403) + typed-405 method discipline + defense-in-depth re-verify (handler re-calls `verifySession` even though proxy already gated, per CB-1.4 Engineer Decision #5). **Engineer DRI Decisions:** (1) extracted `SESSION_COOKIE_NAME` / `SESSION_TTL_SECONDS` / `buildSessionCookie` / `clearSessionCookie` to `lib/auth/cookie.ts` as single source of truth shared with `authenticate/finish` — closes PM Risk #1 (cookie-attribute drift) structurally; (2) typed-405 pattern introduced (existing routes still use Next.js default — retrofit deferred as P3). 17 new Vitest tests + Codex AC 8 E2E (fourth E2E in codebase — sign-out → 200 + Set-Cookie clear → dashboard 302 with `?next=` → second sign-out 401). 3-commit review cycle across 2 rounds: round 1 surfaced 1 BLOCKER (status.md internal-consistency drift, same class as PR #11's v4-era Health hold-out — recurring drift pattern worth watching); round 2 clean. **Codex security review: no findings.** Operator-requested supplemental fresh-Agent Claude security read posted on the PR (4 LOWs, all sub-threshold per Codex). Triggered a 5-PR retrospective A/B (workflow `wzkhajv1x`) comparing fresh-Agent Claude vs Codex on past PRs #1, #2, #5, #8, #10; verdict `mixed-but-leans-supports`. Codex stays binding as security reviewer. Full retrospective at [docs/retros/2026-06-02-security-reviewer-ab.md](retros/2026-06-02-security-reviewer-ab.md).
- **2026-06-01** — [CB-1.4](bets/CB-1/stories/CB-1.4/story.md) shipped via [PR #10](https://github.com/vivekschaudhary/crypto-bot/pull/10). Real session validation in `proxy.ts` (at project root) replacing the scaffold-stub TODO. Closes CB-1 guardrail #1 (zero unauthenticated capital-touching requests) from routing-layer enforcement standpoint. Defense-in-depth posture: downstream handlers MUST re-verify via `verifySession`; forwarded `x-session-*` headers (via Next.js cloned-request-headers + sentinel mechanism) are convenience signals, not auth claims. **5 commits across 4 review rounds** — round 1 surfaced 4 BLOCKERs (file location at `app/proxy.ts` instead of root; invalid `runtime` export; headers-on-response CRITICAL leaking ULIDs to browser; DRI contradiction between "no security gain" and defense-in-depth) + HIGH (open-redirect via `?next`) + 2 MEDIUM (PUBLIC_ROUTES prefix-trap; cookie length cap); rounds 2–3 surfaced cascading prose drift in tech notes, risk mitigations, library API comments, sibling story forward-references, dashboard page placeholder. Both Codex AND Claude fresh-Agent reviewers ran A/B; complementary blind spots validated the multi-model review discipline. 115/115 Vitest tests + AC 8 E2E green locally (third E2E in codebase). Library code unchanged this story (no `lib/auth/sessions` extension); sliding-expiry side-effect explicitly preserved per architecture intent.
- **2026-06-01** — [CB-1.3](bets/CB-1/stories/CB-1.3/story.md) shipped via [PR #8](https://github.com/vivekschaudhary/crypto-bot/pull/8). Passkey authentication ceremony endpoints (`POST /api/auth/authenticate/{begin,finish}`) with user-exists precondition + canonical challenge cookie (via `lib/auth/challenges.mintChallenge`/`consumeChallenge`) + atomic transaction (counter update + rotate-or-create session) + counter-replay guard (Apple 0/0 special case) + OPTIONS preflight handlers. Library extended additively: `rotateSession` + `generateAuthenticationOptions` accept optional pass-through args; new `fromBase64Url` helper preserves caller-minted challenges through SimpleWebAuthn's round-trip. **Second E2E in the codebase** (Codex AC 8, `e2e/auth/authenticate.spec.ts` — Playwright + virtual authenticator + serial DB access via `workers: 1`). 93/93 Vitest tests; both E2E specs green locally. Review cycle surfaced 2 BLOCKERs (Zod schema too loose + AC 8 missing); both closed. Final Codex code + security reviews clean.
- **2026-06-01** — [CB-1.2](bets/CB-1/stories/CB-1.2/story.md) shipped via [PR #5](https://github.com/vivekschaudhary/crypto-bot/pull/5). Passkey registration ceremony endpoints (`POST /api/auth/register/{begin,finish}`) with atomic transactional DB writes + first-time-only gate enforced at API + DB layers (singleton unique index, migration 0002) + origin-check + rate limit + OPTIONS preflight rejection + Engineer DRI Decision for cookie-bound `pendingUserId` (closes Risk #3 by design). 66/66 Vitest tests + **first E2E in the codebase** (Codex AC 8 — Playwright + Chromium virtual authenticator + real Postgres). Review cycle surfaced 5 BLOCKERs across 2 rounds (all closed); 1 follow-up Engineer Risk logged for observability (Sentry hookup is foundation-arch scope, not CB-1.2). `lib/auth/sessions.createSession` extended with optional `tx` parameter for atomic-registration support — additive, backward-compatible.
- **2026-05-31** — [CB-1.1.1](bets/CB-1/stories/CB-1.1.1/story.md) shipped via [PR #2](https://github.com/vivekschaudhary/crypto-bot/pull/2). Review-driven follow-up to CB-1.1: 2 AC amendments (options-object wrapper signatures + ESLint flat-config swap) + 2 missing tests (expired-challenge + happy-path WebAuthn verify) + PR template harden (DO NOT MERGE banner + honest "manually invoked" security-review language) + `.codex/config.toml` fix for Codex 0.133+. 34/34 tests passing; Codex code + security reviews clean.
- **2026-05-31** — [CB-1.1](bets/CB-1/stories/CB-1.1/story.md) shipped via [PR #1](https://github.com/vivekschaudhary/crypto-bot/pull/1). `lib/auth/` library landed: SimpleWebAuthn wrappers, HMAC signed-cookie helpers, DB-backed session helpers, signed-cookie WebAuthn challenge storage. 10 ACs (AC 1 + AC 7 later amended via CB-1.1.1 per Codex findings; original text retained for audit). 31 unit tests at merge.
- **2026-05-31** — [CB-1 brief](bets/CB-1/brief.md) promoted from stub → approved. Passkey authentication; primary metric: sign-in success rate ≥ 99%; 4 guardrails including zero unauthenticated capital-touching requests; `architecture_required: false` (foundation arch covers it).
- **2026-05-31** — [Project Plan](foundation/plan.md) seeded (v1, `status: living`). MVP target: 2026-08-09 (10 weeks); 4-bet critical path; Day-1 parallel pair (CB-1 + CB-2). `docs/dashboard.html` auto-refreshed (218 KB, 12 artifacts).
- **2026-05-31** — [MVP Bet Portfolio](foundation/portfolio.md) approved (`status: approved`). 5 MVP bet stubs created (CB-1..CB-5); Manual trading UI deferred to post-MVP per operator follow-up.
- **2026-05-29** — [Foundational architecture bet](foundation/architecture.md) approved (`status: approved`). Stack: Vercel Pro + Next.js 16 + Supabase Postgres (DB only) + Vercel Cron + SimpleWebAuthn passkey + Sentry free. Scaffold landed; canary green at <https://crypto-bot.kindtree.us>.
- **2026-05-29** — [Foundational product bet](foundation/product.md) approved (`status: approved`). Signal-driven DCA bot for retail Coinbase traders; dry-run-first; single-operator scope; passkey-only auth posture (operator-owned credentials, no third-party IdP).

## Blockers

_None._

## Risks

### Active

- **Strategy edge erosion under regime change** (PM Risk #1 in [foundation/product.md](foundation/product.md)) — mitigated by quarterly Sharpe check-ins against naive-DCA baseline.
- **Operator drift back to emotional trading via manual override** (PM Risk #2) — tracked via Annual KR3 (override rate ≤ 20%).
- **No durable competitive moat** under current personal-product scope (PM Risk #5) — acknowledged + intentional. Any pivot to SaaS triggers a foundation amend.
- **Total loss of all registered passkeys + offline backup code** (PM Risk #7) — mitigated by single-device passkey for MVP + documented absolute-last-resort DB intervention path; multi-device + backup code UX is post-MVP per portfolio.
- **Auto-pause on drawdown + reserve floor deferred to post-MVP** creates a real-money risk window (PM Risk #3 in [foundation/portfolio.md](foundation/portfolio.md)) — bounded by the ≥ 60 dry-run-sessions guardrail before `LIVE_MODE=true`.
- **Session-cookie misconfiguration** in the custom session layer (EA Risk in [foundation/architecture.md](foundation/architecture.md)) — mitigated by DB-row validation on every request + Phase B scaffold hardening checklist.

### Resolved this cycle

- ~~**CB-6 (manual trading) quietly bloating MVP scope**~~ — resolved 2026-05-31 by operator follow-up during portfolio HITL; CB-6 stub deleted; entry moved to portfolio `§ Deliberately out of MVP`.
- ~~**Merge-before-review process slip on PR #1**~~ — Compass Phase 6 (HITL merge) gate fired ~30 min before Phase 5 (Codex review) findings arrived on PR #1 (CB-1.1). Findings (3 BLOCKERs + 1 ISSUE on code; 0 on security) were honestly closed via CB-1.1.1 (2 AC amendments + 2 missing tests). PR template hardened with explicit DO NOT MERGE banner + honest "manually invoked" security-review language. Resolved 2026-05-31 via PR #2. Framework change to `compass/workflows/build.md` Phase 4 wrap-up text deferred to a future retro per operator direction.

## Health

- **Stories shipped:** **7 of 7 under CB-1 (BET COMPLETE).** CB-1.1, CB-1.1.1, CB-1.2, CB-1.3, CB-1.4, CB-1.5, CB-1.6. Plus 1 follow-up security PR (#18). 5 calendar days end-to-end (2026-05-31 → 2026-06-05); 2.3 weeks ahead of plan v5's 06-21 estimate. **5 E2E specs** total (register, authenticate, proxy-gating, sign-out, onboarding). **260 Vitest tests** at CB-1 close. CB-1 guardrail #1 ("zero unauthenticated capital-touching requests") closed bidirectionally; the architecture's "DB row is the source of truth" invariant has both read-side enforcement (proxy gate) and write-side revocation (sign-out endpoint) implemented; the operator-facing UX loop is complete; the audit-flagged DoS surface on pre-auth pages is closed. **CB-1's two PM Open Issues both resolved** (challenge-storage approach via CB-1.2's Engineer DRI; first-deploy onboarding UX via CB-1.6). **First time the brief's `<5-min time-to-first-authenticated-dashboard` guardrail has mechanical assertion** (Playwright virtual authenticator end-to-end, via PR #17 AC 8).
- **Downstream bets unblocked.** CB-2 (signal engine), CB-3 (Coinbase Trade API integration), CB-4 (dry-run safety rails), CB-5 (live-mode promotion) were all gated on CB-1 per the portfolio dependency graph. **CB-2 promotion (`/create-brief CB-2`) is next-on-deck**; the early CB-1 ship opens the window to start CB-2 sooner than plan v5's 2026-06-22 estimated_start. Operator decision: promote CB-2 immediately to capitalize on the velocity, or accept the early-ship as schedule margin and use the window for `/measure` calibration + dependency hardening (no real bet motion).
- **Plan v6 trigger context.** The "Stories created" trigger from v3/v4/v5 has fired for every CB-1 story creation; the "Bet shipped" trigger fires when an entire bet completes. CB-1 just shipped. Plan v6 should recompute (a) CB-2's `estimated_start` (earlier than v5's 06-22 due to CB-1's actuals), (b) MVP target end (potentially earlier than 2026-08-16), (c) cascade through CB-3/4/5. Worth firing `/plan` standalone to capture the new state — separable from this post-merge cleanup.
- **Status.md internal-consistency drift hit again on PR #15.** Same class as PR #11's v4-era Health hold-out (the standing convention from PR #6 that every artifact-state change must sweep status.md across all sections). Twice in two consecutive ship cycles is a pattern — Codex caught it both times. **Forward action:** when shipping a story, the status.md sweep should be a load-bearing checklist item, not a per-Edit afterthought. Worth surfacing in the next `/retro` as a candidate for a status.md-update playbook or a /create-story workflow Postcondition.
- **Process learning captured:** review-before-merge discipline now codified at the PR-template layer (DO NOT MERGE banner). One slip → one named follow-up → one harden, traceable end-to-end via DRI logs.
- **`/plan` v5 refreshed 2026-06-01** — **first MVP-target slip.** CB-1.4's story.md creation fires the "Stories created" trigger and the watch-threshold from v3/v4 hits. Per the [adaptive-decomposition resolution rule](foundation/plan.md#decisions), `duration_weeks = max(5 × 3 days, 2 wk) = max(2.143 wk, 2 wk) = 2.143 wk → rounds up to 3 wk`. CB-1 `estimated_end` 06-14 → 06-21; CB-1 becomes binding-dep for CB-3 (critical-path shift); downstream cascade pushes CB-3/4/5 each +7 days; **MVP target 2026-08-09 → 2026-08-16**. **Forward-watch:** at story count 7+, math stays at 3 wk firmly; at 9+, bumps to 4 wk (MVP would slip again to 2026-08-23). Standing convention from PR #6: every `/plan` refresh sweeps internal consistency across all sections (Currently-in-flight, ## Done, Blocked binding-dep, Full schedule, MVP-target paragraph, Calendar, Refinement log, Risks watch). PR #11 ran this sweep + Codex caught the one v4-era hold-out here at status.md:56 (now closed).
- **`docs/dashboard.html`** regenerates on the next `/dashboard` invocation. **`docs/changelog.md`** Unreleased section still pending a convention decision (strict per-bet vs relaxed per-PR accumulation).
- **New infrastructure in place:** Supabase DB has full schema applied (migrations 0001 + 0002), Playwright + Chromium installed locally, E2E harness operational. CI does not yet run `pnpm e2e` — wiring E2E into CI (with a Supabase test branch + Playwright cache) remains a candidate `/ops` change.
